{
  "name": "system-monitor",
  "displayName": "System Monitor",
  "description": "Shows CPU and RAM on the pulse bar on VSCode, meh, nothing new",
  "version": "0.0.1",
  "publisher": "davidpincheira",
  "engines": { "vscode": "^1.80.0" },
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "systemMonitor.show",
        "title": "Mostrar Uso del Sistema"
      }
    ]
  },
  "dependencies": {
    "os-utils": "^0.0.14"
  }
}

